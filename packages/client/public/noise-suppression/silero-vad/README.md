Silero VAD v5 ONNX model for the voice gate. Downloaded at install/build by
`scripts/fetch-silero-vad-assets.mjs` — not committed. The onnxruntime-web
WASM that runs it is bundled by Vite from node_modules.
